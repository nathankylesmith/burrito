import type { DishTemplate } from './types.js';
export declare const matchReviewDishesToPhotos: (reviewDishes: DishTemplate[], photoDishes: DishTemplate[]) => {
    matchedReviews: DishTemplate[];
    remainingPhotos: DishTemplate[];
};
